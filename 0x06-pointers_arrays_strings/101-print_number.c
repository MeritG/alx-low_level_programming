#include <stdio.h>
#include "main.h"

/**
 * print_number - prints an integer
 * @n: integer
 * Return: 0
 */

 void print_number(int n)
 {
	int num = 0
	       while (num <= 9)
	       {
	putchar(num);
++num;
	       }
	putchar('\n');
	return (0);
 }
